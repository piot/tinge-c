# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_executable(tinge_example
  main.c)

include(Tornado.cmake)
set_tornado(tinge_example)

target_include_directories(tinge_example PRIVATE ../include)


target_link_libraries(tinge_example PUBLIC
  tinge)

