# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(tinge STATIC 
  tinge.c)

include(Tornado.cmake)
set_tornado(tinge)

target_include_directories(tinge PUBLIC ../include)


target_link_libraries(tinge PUBLIC 
  flood)

